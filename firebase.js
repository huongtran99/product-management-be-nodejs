const admin = require('firebase-admin');
admin.initializeApp({
    credential: admin.credential.cert(nam5 (us-central)),
    storageBucket: file-image-product.appspot.com
})

const bucket = admin.storage().bucket()

module.exports = {
    bucket
}
